CREATE TABLE PlatoonLog (
    uniqueId        BIGINT NOT NULL,
    power           INT NOT NULL, 
    unitId          INT NOT NULL,
    characterid     INT NOT NULL,
    characterName   VARCHAR(128)
);